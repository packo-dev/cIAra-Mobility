-- ============================================
-- QUÊTE 1.4 : Afficher les véhicules ayant une autonomie supérieure à 400 km
-- Notions : SELECT, FROM, WHERE
-- ============================================

SELECT *
FROM vehicule
WHERE autonomie_km > 400;

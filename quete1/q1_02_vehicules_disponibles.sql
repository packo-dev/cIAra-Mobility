-- ============================================
-- QUÊTE 1.2 : Afficher uniquement les véhicules disponibles
-- Notions : SELECT, FROM, WHERE
-- ============================================

SELECT *
FROM vehicule
WHERE etat = 'Disponible';

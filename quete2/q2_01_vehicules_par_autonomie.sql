-- ============================================
-- QUÊTE 2.1 : Afficher les véhicules triés par autonomie décroissante
-- Notions : SELECT, FROM, ORDER BY DESC
-- ============================================

SELECT *
FROM vehicule
ORDER BY autonomie_km DESC;
